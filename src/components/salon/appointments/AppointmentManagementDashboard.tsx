'use client'
export function AppointmentManagementDashboard({ organizationId }: { organizationId?: string }) {
  return <div>Appointment Management Dashboard for {organizationId}</div>
}

export default AppointmentManagementDashboard

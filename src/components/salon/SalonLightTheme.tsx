'use client'

import React from 'react'
import SalonPage from '@/app/salon/page'

// This component wraps the original salon page (light theme)
export default function SalonLightTheme() {
  return <SalonPage />
}

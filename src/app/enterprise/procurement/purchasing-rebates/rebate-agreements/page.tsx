'use client'

/**
 * Rebate Agreements CRUD Page
 * Generated by HERA Enterprise Generator
 * 
 * Module: PROCUREMENT
 * Entity: REBATE_AGREEMENT
 * Smart Code: HERA.PURCHASE.REBATE.AGREEMENT.ENTITY.v1
 * Description: Vendor rebate agreements and terms
 */

import React, { useState, useCallback, useEffect } from 'react'
import { MobilePageLayout } from '@/components/mobile/MobilePageLayout'
import { MobileFilters, type FilterField } from '@/components/mobile/MobileFilters'
import { MobileDataTable, type TableColumn, type TableRecord } from '@/components/mobile/MobileDataTable'
import { MobileCard } from '@/components/mobile/MobileCard'
import { MobileChart } from '@/components/mobile/MobileCharts'
import { useHERAAuth } from '@/components/auth/HERAAuthProvider'
import { useUniversalEntity } from '@/hooks/useUniversalEntity'
import { 
  AlertCircle,
  CheckCircle,
  Clock,
  Download,
  Edit,
  Eye,
  FileText,
  Filter,
  MoreVertical,
  Plus,
  Save,
  Search,
  Trash2,
  TrendingUp,
  Upload,
  X
} from 'lucide-react'

/**
 * RebateAgreement Entity Interface
 * Extends TableRecord for HERA compliance
 */
interface RebateAgreement extends TableRecord {
  id: string
  entity_id?: string
  entity_name: string
  smart_code: string
  status?: string
  
  // Dynamic fields (stored in core_dynamic_data)
  vendor_id?: string
  product_category?: string
  rebate_percentage?: string
  min_volume?: string
  start_date?: string
  end_date?: string
  
  // System fields
  created_at?: string
  updated_at?: string
  created_by?: string
  updated_by?: string
  
  // Business rule fields
  
  
}

/**
 * HERA Rebate Agreement Smart Codes
 * Auto-generated from preset configuration
 */
const REBATE_AGREEMENT_SMART_CODES = {
  ENTITY: 'HERA.PURCHASE.REBATE.AGREEMENT.ENTITY.v1',
  FIELD_VENDOR_ID: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.VENDOR_ID.V1',
  FIELD_PRODUCT_CATEGORY: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.PRODUCT_CATEGORY.V1',
  FIELD_REBATE_PERCENTAGE: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.REBATE_PERCENTAGE.V1',
  FIELD_MIN_VOLUME: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.MIN_VOLUME.V1',
  FIELD_START_DATE: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.START_DATE.V1',
  FIELD_END_DATE: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.END_DATE.V1',
  FIELD_STATUS: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.STATUS.V1',
  
  // Event smart codes for audit trail
  EVENT_CREATED: 'HERA.PURCHASE.REBATE.AGREEMENT.EVENT.v1.CREATED.V1',
  EVENT_UPDATED: 'HERA.PURCHASE.REBATE.AGREEMENT.EVENT.v1.UPDATED.V1',
  EVENT_DELETED: 'HERA.PURCHASE.REBATE.AGREEMENT.EVENT.v1.DELETED.V1'
} as const

/**
 * Rebate Agreements Main Page Component
 * Enterprise-grade CRUD with quality gates and business rules
 */
export default function RebateAgreementsPage() {
  const { currentOrganization, isAuthenticated, user } = useHERAAuth()
  const [selectedRebateAgreements, setSelectedRebateAgreements] = useState<(string | number)[]>([])
  const [showAddModal, setShowAddModal] = useState(false)
  const [showEditModal, setShowEditModal] = useState(false)
  const [showDeleteModal, setShowDeleteModal] = useState(false)
  const [currentRebateAgreement, setCurrentRebateAgreement] = useState<RebateAgreement | null>(null)
  const [rebate_agreementToDelete, setRebateAgreementToDelete] = useState<RebateAgreement | null>(null)
  const [filters, setFilters] = useState({
    search: '',
    status: '',
    // Dynamic filter fields
    vendor_id: '',
    product_category: '',
    rebate_percentage: ''
  })

  // HERA Universal Entity Integration
  const rebate_agreementData = useUniversalEntity({
    entity_type: 'REBATE_AGREEMENT',
    organizationId: currentOrganization?.id,
    filters: {
      include_dynamic: true,
      include_relationships: undefined,
      status: 'active'
    },
    dynamicFields: [
      { name: 'vendor_id', type: 'text', smart_code: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.VENDOR_ID.V1', required: false },
      { name: 'product_category', type: 'text', smart_code: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.PRODUCT_CATEGORY.V1', required: false },
      { name: 'rebate_percentage', type: 'text', smart_code: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.REBATE_PERCENTAGE.V1', required: false },
      { name: 'min_volume', type: 'text', smart_code: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.MIN_VOLUME.V1', required: false },
      { name: 'start_date', type: 'date', smart_code: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.START_DATE.V1', required: false },
      { name: 'end_date', type: 'date', smart_code: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.END_DATE.V1', required: false },
      { name: 'status', type: 'text', smart_code: 'HERA.PURCHASE.REBATE.AGREEMENT.DYN.v1.STATUS.V1', required: false }
    ]
  })

  // Transform entities with business rule extensions
  const rebate_agreements: RebateAgreement[] = rebate_agreementData.entities?.map((entity: any) => {
    return {
      id: entity.id,
      entity_id: entity.id,
      entity_name: entity.entity_name || '',
      smart_code: entity.smart_code || '',
      status: entity.status || 'active',
      
      // Map dynamic fields with type safety
      vendor_id: entity.dynamic_data?.find((d: any) => d.field_name === 'vendor_id')?.field_value_text || '',
      product_category: entity.dynamic_data?.find((d: any) => d.field_name === 'product_category')?.field_value_text || '',
      rebate_percentage: entity.dynamic_data?.find((d: any) => d.field_name === 'rebate_percentage')?.field_value_text || '',
      min_volume: entity.dynamic_data?.find((d: any) => d.field_name === 'min_volume')?.field_value_text || '',
      start_date: entity.dynamic_data?.find((d: any) => d.field_name === 'start_date')?.field_value_text || '',
      end_date: entity.dynamic_data?.find((d: any) => d.field_name === 'end_date')?.field_value_text || '',
      
      // System fields
      created_at: entity.created_at,
      updated_at: entity.updated_at,
      created_by: entity.created_by,
      updated_by: entity.updated_by
      
      
    }
  }) || []

  // Enhanced KPI calculations with preset metrics
  const kpis = [
    {
      title: 'Total Rebate Agreements',
      value: rebate_agreements.length.toString(),
      change: '+5.2%',
      trend: 'up' as const,
      icon: FileText
    },
    {
      title: 'Active Rebate Agreements',
      value: rebate_agreements.filter(item => item.status === 'active').length.toString(),
      change: '+2.1%',
      trend: 'up' as const,
      icon: CheckCircle
    },
    {
      title: 'This Month',
      value: rebate_agreements.filter(item => {
        if (!item.created_at) return false
        const created = new Date(item.created_at)
        const now = new Date()
        return created.getMonth() === now.getMonth() && created.getFullYear() === now.getFullYear()
      }).length.toString(),
      change: '+8.3%',
      trend: 'up' as const,
      icon: TrendingUp
    }
  ]

  // Enhanced table columns with business rule columns
  const columns: TableColumn[] = [
    { key: 'entity_name', label: 'Rebate Agreement Name', sortable: true },
    { key: 'vendor_id', label: 'Vendor id', sortable: true },
    { key: 'product_category', label: 'Product category', sortable: true },
    { key: 'rebate_percentage', label: 'Rebate percentage', sortable: true },
    { key: 'min_volume', label: 'Min volume', sortable: true },
    { key: 'start_date', label: 'Start date', sortable: true },
    { key: 'created_at', label: 'Created', sortable: true },
    { key: 'actions', label: 'Actions', sortable: false }
  ]

  // Enhanced filter fields with business rule filters
  const filterFields: FilterField[] = [
    { key: 'search', label: 'Search Rebate Agreements', type: 'search' },
    { key: 'status', label: 'Status', type: 'select', options: [
      { value: '', label: 'All Status' },
      { value: 'active', label: 'Active' },
      { value: 'inactive', label: 'Inactive' }
    ]},
    { key: 'vendor_id', label: 'Vendor Id', type: 'select', options: [
        { value: '', label: 'All Vendor Ids' },
        ...Array.from(new Set(rebate_agreements.map(item => item.vendor_id).filter(Boolean))).map(val => ({ value: val!, label: val! }))
      ]},
    { key: 'product_category', label: 'Product Category', type: 'select', options: [
        { value: '', label: 'All Product Categorys' },
        ...Array.from(new Set(rebate_agreements.map(item => item.product_category).filter(Boolean))).map(val => ({ value: val!, label: val! }))
      ]}
  ]

  // Enterprise CRUD Operations with Events
  const handleAddRebateAgreement = async (rebate_agreementData: any) => {
    try {
      const result = await rebate_agreementData.create({
        entity_type: 'REBATE_AGREEMENT',
        entity_name: rebate_agreementData.entity_name,
        smart_code: REBATE_AGREEMENT_SMART_CODES.ENTITY,
        organization_id: currentOrganization?.id
      }, rebate_agreementData)

      // Emit creation event for audit trail
      await rebate_agreementData.emitEvent(REBATE_AGREEMENT_SMART_CODES.EVENT_CREATED, {
        entity_id: result.id,
        user_id: user?.id,
        timestamp: new Date().toISOString(),
        data: rebate_agreementData
      })

      setShowAddModal(false)
      console.log('✅ Rebate Agreement created successfully')
    } catch (error) {
      console.error('❌ Error adding rebate_agreement:', error)
    }
  }

  const handleEditRebateAgreement = async (rebate_agreementData: any) => {
    if (!currentRebateAgreement) return
    
    try {
      await rebate_agreementData.update(currentRebateAgreement.entity_id!, {
        entity_name: rebate_agreementData.entity_name
      }, rebate_agreementData)

      // Emit update event
      await rebate_agreementData.emitEvent(REBATE_AGREEMENT_SMART_CODES.EVENT_UPDATED, {
        entity_id: currentRebateAgreement.entity_id!,
        user_id: user?.id,
        timestamp: new Date().toISOString(),
        changes: rebate_agreementData
      })

      setShowEditModal(false)
      setCurrentRebateAgreement(null)
      console.log('✅ Rebate Agreement updated successfully')
    } catch (error) {
      console.error('❌ Error updating rebate_agreement:', error)
    }
  }

  const handleDeleteRebateAgreement = async () => {
    if (!rebate_agreementToDelete) return
    
    try {
      await rebate_agreementData.delete(rebate_agreementToDelete.entity_id!)

      // Emit deletion event
      await rebate_agreementData.emitEvent(REBATE_AGREEMENT_SMART_CODES.EVENT_DELETED, {
        entity_id: rebate_agreementToDelete.entity_id!,
        user_id: user?.id,
        timestamp: new Date().toISOString(),
        entity_name: rebate_agreementToDelete.entity_name
      })

      setShowDeleteModal(false)
      setRebateAgreementToDelete(null)
      console.log('✅ Rebate Agreement deleted successfully')
    } catch (error) {
      console.error('❌ Error deleting rebate_agreement:', error)
    }
  }

  
  

  // Enterprise security checks
  if (!isAuthenticated) {
    return (
      <div className="p-4 text-center">
        <AlertCircle className="h-8 w-8 text-red-500 mx-auto mb-2" />
        <p>Please log in to access Rebate Agreements.</p>
      </div>
    )
  }

  if (rebate_agreementData.contextLoading) {
    return (
      <div className="p-4 text-center">
        <div className="animate-spin h-8 w-8 border-4 border-blue-500 border-t-transparent rounded-full mx-auto mb-2"></div>
        <p>Loading Rebate Agreements...</p>
      </div>
    )
  }

  if (!currentOrganization) {
    return (
      <div className="p-4 text-center">
        <AlertCircle className="h-8 w-8 text-yellow-500 mx-auto mb-2" />
        <p>No organization context found. Please select an organization.</p>
      </div>
    )
  }

  return (
    <MobilePageLayout
      title="Rebate Agreements"
      subtitle={`${rebate_agreements.length} total rebate_agreements`}
      primaryColor="#d83b01"
      accentColor="#a52d00"
      showBackButton={false}
    >
      {/* Enterprise KPI Dashboard */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
        {kpis.map((kpi, index) => (
          <MobileCard key={index} className="p-4 hover:shadow-md transition-shadow">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm text-gray-600 font-medium">{kpi.title}</p>
                <p className="text-2xl font-bold" style={{ color: '#d83b01' }}>{kpi.value}</p>
                <p className={`text-xs font-medium ${kpi.trend === 'up' ? 'text-green-600' : 'text-red-600'}`}>
                  {kpi.change}
                </p>
              </div>
              <kpi.icon className="h-8 w-8 text-gray-400" />
            </div>
          </MobileCard>
        ))}
      </div>

      {/* Enhanced Filters */}
      <MobileFilters
        fields={filterFields}
        values={filters}
        onChange={setFilters}
        className="mb-6"
      />

      {/* Enterprise Data Table */}
      <MobileDataTable
        data={rebate_agreements}
        columns={columns}
        selectedRows={selectedRebateAgreements}
        onRowSelect={setSelectedRebateAgreements}
        onRowClick={(rebate_agreement) => {
          setCurrentRebateAgreement(rebate_agreement)
          setShowEditModal(true)
        }}
        showBulkActions={selectedRebateAgreements.length > 0}
        bulkActions={[
          {
            label: 'Delete Selected',
            action: async () => {
              // Bulk delete with events
              for (const id of selectedRebateAgreements) {
                await rebate_agreementData.delete(id.toString())
              }
              setSelectedRebateAgreements([])
            },
            variant: 'destructive'
          }
        ]}
        mobileCardRender={(rebate_agreement) => (
          <MobileCard key={rebate_agreement.id} className="p-4 hover:shadow-md transition-shadow">
            <div className="flex justify-between items-start mb-3">
              <div>
                <h3 className="font-semibold text-lg">{rebate_agreement.entity_name}</h3>
                
                
              </div>
              <div className="flex space-x-2">
                <button
                  onClick={() => {
                    setCurrentRebateAgreement(rebate_agreement)
                    setShowEditModal(true)
                  }}
                  className="p-1 text-blue-600 hover:bg-blue-50 rounded"
                >
                  <Edit className="h-4 w-4" />
                </button>
                <button
                  onClick={() => {
                    setRebateAgreementToDelete(rebate_agreement)
                    setShowDeleteModal(true)
                  }}
                  className="p-1 text-red-600 hover:bg-red-50 rounded"
                >
                  <Trash2 className="h-4 w-4" />
                </button>
              </div>
            </div>
            
            {/* Dynamic fields display */}
            <div className="text-sm text-gray-600 mb-1">
              <span className="font-medium">Vendor Id:</span>{' '}
              {rebate_agreement.vendor_id || 'N/A'}
            </div>
            <div className="text-sm text-gray-600 mb-1">
              <span className="font-medium">Product Category:</span>{' '}
              {rebate_agreement.product_category || 'N/A'}
            </div>
            <div className="text-sm text-gray-600 mb-1">
              <span className="font-medium">Rebate Percentage:</span>{' '}
              {rebate_agreement.rebate_percentage || 'N/A'}
            </div>
            <div className="text-sm text-gray-600 mb-1">
              <span className="font-medium">Min Volume:</span>{' '}
              {rebate_agreement.min_volume || 'N/A'}
            </div>
            
            <div className="text-xs text-gray-400 mt-2 pt-2 border-t">
              Created: {rebate_agreement.created_at ? new Date(rebate_agreement.created_at).toLocaleDateString() : 'N/A'}
            </div>
          </MobileCard>
        )}
      />

      {/* Floating Action Button */}
      <button
        onClick={() => setShowAddModal(true)}
        className="fixed bottom-6 right-6 text-white rounded-full p-4 shadow-lg transition-colors z-50 hover:shadow-xl"
        style={{ backgroundColor: '#d83b01' }}
      >
        <Plus className="h-6 w-6" />
      </button>

      {/* Enterprise Modals */}
      {showAddModal && (
        <RebateAgreementModal
          title="Add New Rebate Agreement"
          isOpen={showAddModal}
          onClose={() => setShowAddModal(false)}
          onSave={handleAddRebateAgreement}
          dynamicFields={rebate_agreementData.dynamicFieldsConfig || []}
          businessRules={{"date_validation":true,"audit_trail":true,"approval_workflow":true}}
        />
      )}

      {showEditModal && currentRebateAgreement && (
        <RebateAgreementModal
          title="Edit Rebate Agreement"
          isOpen={showEditModal}
          onClose={() => {
            setShowEditModal(false)
            setCurrentRebateAgreement(null)
          }}
          onSave={handleEditRebateAgreement}
          initialData={currentRebateAgreement}
          dynamicFields={rebate_agreementData.dynamicFieldsConfig || []}
          businessRules={{"date_validation":true,"audit_trail":true,"approval_workflow":true}}
        />
      )}

      {showDeleteModal && rebate_agreementToDelete && (
        <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50">
          <div className="bg-white rounded-lg max-w-md w-full p-6">
            <div className="flex items-center mb-4">
              <AlertCircle className="h-6 w-6 text-red-500 mr-2" />
              <h3 className="text-lg font-semibold">Delete Rebate Agreement</h3>
            </div>
            <p className="text-gray-600 mb-6">
              Are you sure you want to delete "{rebate_agreementToDelete.entity_name}"? This action cannot be undone.
            </p>
            <div className="flex justify-end space-x-3">
              <button
                onClick={() => {
                  setShowDeleteModal(false)
                  setRebateAgreementToDelete(null)
                }}
                className="px-4 py-2 text-gray-600 border border-gray-300 rounded-md hover:bg-gray-50"
              >
                Cancel
              </button>
              <button
                onClick={handleDeleteRebateAgreement}
                className="px-4 py-2 bg-red-600 text-white rounded-md hover:bg-red-700 flex items-center"
              >
                <Trash2 className="h-4 w-4 mr-2" />
                Delete
              </button>
            </div>
          </div>
        </div>
      )}
    </MobilePageLayout>
  )
}

/**
 * Enterprise Rebate Agreement Modal Component
 * Enhanced with business rules and validation
 */
interface RebateAgreementModalProps {
  title: string
  isOpen: boolean
  onClose: () => void
  onSave: (data: any) => void
  initialData?: RebateAgreement
  dynamicFields: any[]
  businessRules: any
}

function RebateAgreementModal({ 
  title, 
  isOpen, 
  onClose, 
  onSave, 
  initialData, 
  dynamicFields,
  businessRules 
}: RebateAgreementModalProps) {
  const [formData, setFormData] = useState(() => {
    const initial: any = { 
      entity_name: initialData?.entity_name || '' 
    }
    
    dynamicFields.forEach(field => {
      initial[field.name] = initialData?.[field.name as keyof RebateAgreement] || (field.type === 'number' ? 0 : '')
    })
    
    return initial
  })

  const [errors, setErrors] = useState<Record<string, string>>({})
  const [isSubmitting, setIsSubmitting] = useState(false)

  const validateForm = () => {
    const newErrors: Record<string, string> = {}
    
    // Validate required fields
    dynamicFields.forEach(field => {
      if (field.required && !formData[field.name]) {
        newErrors[field.name] = `${field.label || field.name} is required`
      }
    })
    
    // Entity name validation
    if (!formData.entity_name?.trim()) {
      newErrors.entity_name = 'Rebate Agreement name is required'
    }
    
    setErrors(newErrors)
    return Object.keys(newErrors).length === 0
  }

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    
    if (!validateForm()) return
    
    setIsSubmitting(true)
    try {
      await onSave(formData)
    } catch (error) {
      console.error('Form submission error:', error)
    } finally {
      setIsSubmitting(false)
    }
  }

  if (!isOpen) return null

  return (
    <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50">
      <div className="bg-white rounded-lg max-w-2xl w-full max-h-[90vh] overflow-y-auto">
        <div className="p-6">
          <div className="flex justify-between items-center mb-6">
            <h3 className="text-xl font-semibold">{title}</h3>
            <button onClick={onClose} className="text-gray-400 hover:text-gray-600">
              <X className="h-6 w-6" />
            </button>
          </div>

          <form onSubmit={handleSubmit} className="space-y-4">
            {/* Entity Name Field */}
            <div>
              <label className="block text-sm font-medium text-gray-700 mb-1">
                Rebate Agreement Name *
              </label>
              <input
                type="text"
                value={formData.entity_name}
                onChange={(e) => setFormData({ ...formData, entity_name: e.target.value })}
                className={`w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 ${errors.entity_name ? 'border-red-300 focus:ring-red-500' : 'border-gray-300 focus:ring-blue-500'}`}
                required
                disabled={isSubmitting}
              />
              {errors.entity_name && (
                <p className="mt-1 text-sm text-red-600">{errors.entity_name}</p>
              )}
            </div>

            {/* Dynamic Fields with Enhanced Validation */}
            
            <div>
              <label className="block text-sm font-medium text-gray-700 mb-1">
                Vendor Id 
              </label>
              <input
                type="text"
                value={formData.vendor_id}
                onChange={(e) => setFormData({ ...formData, vendor_id: e.target.value })}
                className={`w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 ${errors.vendor_id ? 'border-red-300 focus:ring-red-500' : 'border-gray-300 focus:ring-blue-500'}`}
                
                disabled={isSubmitting}
              />
              {errors.vendor_id && (
                <p className="mt-1 text-sm text-red-600">{errors.vendor_id}</p>
              )}
            </div>
            
            <div>
              <label className="block text-sm font-medium text-gray-700 mb-1">
                Product Category 
              </label>
              <input
                type="text"
                value={formData.product_category}
                onChange={(e) => setFormData({ ...formData, product_category: e.target.value })}
                className={`w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 ${errors.product_category ? 'border-red-300 focus:ring-red-500' : 'border-gray-300 focus:ring-blue-500'}`}
                
                disabled={isSubmitting}
              />
              {errors.product_category && (
                <p className="mt-1 text-sm text-red-600">{errors.product_category}</p>
              )}
            </div>
            
            <div>
              <label className="block text-sm font-medium text-gray-700 mb-1">
                Rebate Percentage 
              </label>
              <input
                type="text"
                value={formData.rebate_percentage}
                onChange={(e) => setFormData({ ...formData, rebate_percentage: e.target.value })}
                className={`w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 ${errors.rebate_percentage ? 'border-red-300 focus:ring-red-500' : 'border-gray-300 focus:ring-blue-500'}`}
                
                disabled={isSubmitting}
              />
              {errors.rebate_percentage && (
                <p className="mt-1 text-sm text-red-600">{errors.rebate_percentage}</p>
              )}
            </div>
            
            <div>
              <label className="block text-sm font-medium text-gray-700 mb-1">
                Min Volume 
              </label>
              <input
                type="text"
                value={formData.min_volume}
                onChange={(e) => setFormData({ ...formData, min_volume: e.target.value })}
                className={`w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 ${errors.min_volume ? 'border-red-300 focus:ring-red-500' : 'border-gray-300 focus:ring-blue-500'}`}
                
                disabled={isSubmitting}
              />
              {errors.min_volume && (
                <p className="mt-1 text-sm text-red-600">{errors.min_volume}</p>
              )}
            </div>
            
            <div>
              <label className="block text-sm font-medium text-gray-700 mb-1">
                Start Date 
              </label>
              <input
                type="date"
                value={formData.start_date}
                onChange={(e) => setFormData({ ...formData, start_date: e.target.value })}
                className={`w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 ${errors.start_date ? 'border-red-300 focus:ring-red-500' : 'border-gray-300 focus:ring-blue-500'}`}
                
                disabled={isSubmitting}
              />
              {errors.start_date && (
                <p className="mt-1 text-sm text-red-600">{errors.start_date}</p>
              )}
            </div>
            
            <div>
              <label className="block text-sm font-medium text-gray-700 mb-1">
                End Date 
              </label>
              <input
                type="date"
                value={formData.end_date}
                onChange={(e) => setFormData({ ...formData, end_date: e.target.value })}
                className={`w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 ${errors.end_date ? 'border-red-300 focus:ring-red-500' : 'border-gray-300 focus:ring-blue-500'}`}
                
                disabled={isSubmitting}
              />
              {errors.end_date && (
                <p className="mt-1 text-sm text-red-600">{errors.end_date}</p>
              )}
            </div>
            
            <div>
              <label className="block text-sm font-medium text-gray-700 mb-1">
                Status 
              </label>
              <input
                type="text"
                value={formData.status}
                onChange={(e) => setFormData({ ...formData, status: e.target.value })}
                className={`w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 ${errors.status ? 'border-red-300 focus:ring-red-500' : 'border-gray-300 focus:ring-blue-500'}`}
                
                disabled={isSubmitting}
              />
              {errors.status && (
                <p className="mt-1 text-sm text-red-600">{errors.status}</p>
              )}
            </div>

            {/* Business Rules Info */}
            

            {/* Form Actions */}
            <div className="flex justify-end space-x-3 pt-4 border-t">
              <button
                type="button"
                onClick={onClose}
                disabled={isSubmitting}
                className="px-4 py-2 text-gray-600 border border-gray-300 rounded-md hover:bg-gray-50 disabled:opacity-50"
              >
                Cancel
              </button>
              <button
                type="submit"
                disabled={isSubmitting}
                className="px-4 py-2 text-white rounded-md hover:opacity-90 flex items-center disabled:opacity-50"
                style={{ backgroundColor: '#d83b01' }}
              >
                {isSubmitting ? (
                  <>
                    <div className="animate-spin h-4 w-4 border-2 border-white border-t-transparent rounded-full mr-2"></div>
                    Saving...
                  </>
                ) : (
                  <>
                    <Save className="h-4 w-4 mr-2" />
                    Save
                  </>
                )}
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  )
}
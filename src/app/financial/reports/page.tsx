'use client'

import { useState, useEffect } from 'react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import { 
  Package, Users, BarChart3, Settings 
} from 'lucide-react'

/**
 * Universal financial management module - reports Page
 * Generated by HERA Module Generator
 * 
 * Universal UI following HERA design patterns
 * Layer 4 of 7-Layer Build Standard
 */

export default function FinancialReportsPage() {
  const [data, setData] = useState([])
  const [loading, setLoading] = useState(false)

  const moduleStats = [
    {
      title: 'Active items',
      value: '1,247',
      icon: Package,
      color: 'blue',
      change: '+12%'
    },
    {
      title: 'Total creates',
      value: '8,932',
      icon: BarChart3,
      color: 'emerald',
      change: '+18%'
    },
    {
      title: 'Active Users',
      value: '156',
      icon: Users,
      color: 'purple',
      change: '+5%'
    },
    {
      title: 'System Health',
      value: '99.9%',
      icon: Settings,
      color: 'cyan',
      change: '+0.1%'
    }
  ]

  useEffect(() => {
    // Load module data using universal API patterns
    loadModuleData()
  }, [])

  const loadModuleData = async () => {
    setLoading(true)
    try {
      // Use HERA universal API
      const response = await fetch(`/api/v1/financial/reports?organization_id=${organizationId}`)
      const result = await response.json()
      setData(result.data || [])
    } catch (error) {
      console.error('Error loading financial data:', error)
    }
    setLoading(false)
  }

  return (
    <div className="min-h-screen bg-background">
      <div className="container mx-auto p-6 max-w-7xl">
        {/* Header */}
        <div className="mb-8">
          <div className="flex items-center justify-between">
            <div>
              <h1 className="text-4xl font-bold bg-gradient-to-r from-blue-400 to-blue-600 bg-clip-text text-transparent">
                FINANCIAL Reports
              </h1>
              <p className="text-muted-foreground mt-2">
                Universal financial management module - Universal HERA Module
              </p>
            </div>
            <Badge className="bg-blue-500/20 text-blue-400 border-blue-500/30">
              HERA.FIN.REPORTS.v1
            </Badge>
          </div>
        </div>

        {/* Stats Grid */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
          {moduleStats.map((stat, index) => {
            const IconComponent = eval(stat.icon)
            return (
              <Card key={index} className="relative overflow-hidden">
                <CardContent className="p-6">
                  <div className="flex items-center justify-between">
                    <div>
                      <p className="text-sm text-muted-foreground">{stat.title}</p>
                      <p className="text-2xl font-bold">{stat.value}</p>
                      <p className="text-sm text-emerald-400">{stat.change}</p>
                    </div>
                    <div className={`w-12 h-12 bg-gradient-to-r from-${stat.color}-400 to-${stat.color}-600 rounded-lg flex items-center justify-center`}>
                      <IconComponent className="w-6 h-6 text-white" />
                    </div>
                  </div>
                </CardContent>
              </Card>
            )
          })}
        </div>

        {/* Main Content */}
        <Card>
          <CardHeader>
            <CardTitle className="flex items-center gap-2">
              <Package className="w-5 h-5" />
              Reports Management
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-center py-12">
              <div className="w-16 h-16 bg-gradient-to-r from-blue-400 to-blue-600 rounded-2xl mx-auto mb-4 flex items-center justify-center">
                <Package className="w-8 h-8 text-white" />
              </div>
              <h3 className="text-xl font-semibold mb-2">FINANCIAL Module Ready</h3>
              <p className="text-muted-foreground mb-6">
                Generated using HERA's 7-Layer Build Standard.<br/>
                Universal architecture with industry-specific patterns.
              </p>
              <div className="flex justify-center space-x-4">
                <Button className="bg-blue-500 hover:bg-blue-600">
                  Start Using Financial
                </Button>
                <Button variant="outline">
                  View Documentation
                </Button>
              </div>
            </div>
          </CardContent>
        </Card>

        {/* HERA Advantage */}
        <Card className="mt-8 bg-gradient-to-r from-blue-500/10 to-emerald-500/10">
          <CardContent className="p-6">
            <h3 className="text-lg font-semibold mb-4">🎯 HERA Advantage</h3>
            <div className="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
              <div>
                <strong className="text-blue-400">6 Days vs 6+ Months:</strong><br/>
                Traditional module development takes 26-52 weeks. HERA's universal architecture completes modules in 6 days.
              </div>
              <div>
                <strong className="text-emerald-400">Universal Foundation:</strong><br/>
                Built on HERA's 6-table architecture. No schema changes needed for any business complexity.
              </div>
              <div>
                <strong className="text-purple-400">AI-Ready:</strong><br/>
                Smart codes and universal patterns prepare your module for AI-powered business intelligence.
              </div>
            </div>
          </CardContent>
        </Card>
      </div>
    </div>
  )
}

smart_code: HERA.INVENTORY.MDM.DYNAMIC_KEYS.CORE.V1
items:
  # Item
  - { slug: item.sku,              entity_type: ENTITY, name: "SKU",               metadata: { type: "text" } }
  - { slug: item.category,         entity_type: ENTITY, name: "Category",          metadata: { type: "text" } }
  - { slug: item.base_uom,         entity_type: ENTITY, name: "Base UOM",          metadata: { type: "text" } }
  - { slug: item.standard_cost,    entity_type: ENTITY, name: "Standard Cost",     metadata: { type: "number" } }
  - { slug: item.safety_stock,     entity_type: ENTITY, name: "Safety Stock",      metadata: { type: "number" } }
  - { slug: item.reorder_point,    entity_type: ENTITY, name: "Reorder Point",     metadata: { type: "number" } }

  # Location
  - { slug: location.location_code, entity_type: ENTITY, name: "Location Code",     metadata: { type: "text" } }
  - { slug: location.type,          entity_type: ENTITY, name: "Location Type",     metadata: { type: "text", allowed: ["warehouse","store","bin"] } }

  # UOM
  - { slug: uom.conversion_to_base, entity_type: ENTITY, name: "Conversion to Base", metadata: { type: "number" } }

  # Lot
  - { slug: lot.expiry_date,        entity_type: ENTITY, name: "Expiry Date",       metadata: { type: "date" } }

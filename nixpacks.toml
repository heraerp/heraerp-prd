# nixpacks.toml
providers = ["node"]

[variables]
NODE_ENV = "production"

[phases.install]
cmds = ["ls -la /app", "pwd", "npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "next start -H 0.0.0.0 -p $PORT"
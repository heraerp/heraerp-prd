# Force Node.js detection and prevent Deno usage
providers = ["node"]

[variables]
NODE_VERSION = "20.18.0"
NODE_ENV = "production"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build:railway"]
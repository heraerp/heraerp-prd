# nixpacks.toml
providers = ["node"]

[variables]
NODE_ENV = "production"

[phases.install]
cmds = ["ls -la /app", "pwd", "npm ci --omit=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:railway"
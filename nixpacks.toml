[phases.setup]
nixPkgs = ["nodejs-20_x", "bash"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["chmod +x ./scripts/railway-build.sh", "./scripts/railway-build.sh"]

[start]
cmd = "chmod +x ./scripts/railway-start.sh && ./scripts/railway-start.sh"
EOF < /dev/null
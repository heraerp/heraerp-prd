# nixpacks.toml

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
# Install all dependencies including devDependencies for build
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"

[variables]
# Set NODE_ENV after build to ensure devDependencies are available during build
NODE_ENV = "production"
# Do NOT set PORT here - Railway injects it
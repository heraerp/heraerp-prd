[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["node scripts/railway-deploy.js build"]

[start]
cmd = "node scripts/railway-deploy.js start"